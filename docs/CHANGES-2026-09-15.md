# 1.0.7 单屏操作与 24fps

重做银白月弧与蓝色光点图标。预览区域自适应剩余高度，启用和立即运行固定底部同一行；移除设置按钮，不需滚动操作。

先提供用户已允许的 A/C 两段体验，每次等概率选择一次并固定循环。两段转为 24fps 无声 HEVC；窗口请求 24Hz，实际面板刷新率取决于硬件支持，尚未验证。每段仍不超过 12 秒。

立即运行明确指定 Dream 组件。针对 63 将 Dream 主窗口放在 D2 而普通 Presentation 不能放到 D0 的实际错误，新增仅应用内部可启动的普通 D0 Activity，共享播放器和退出清理，不申请悬浮窗权限。这是已编译的候选修复，63 拒绝连接，不能宣称已经解决实机退出。

签名沿用用户指定的 priv/xtqx.md 平台证书；构建、资源与视频解码检查通过。设备上仍是此前安装的 1.0.6，本版未安装。
