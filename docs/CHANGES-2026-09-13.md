# 2026-09-13：1.0.2 三选一固定循环

独立 DreamService 屏保；原壁纸工程与 32 套素材不变。每次启动等概率随机选择 A/B/C 一次，之后持续循环该视频，退出后下一次启动再随机。双屏共用同一播放器、纹理和媒体时间线，不再跨视频交替。

A 为原 set32 的逐字节副本。B 指定抖音源尚未取得。C 已保留最高可取 1080×1920 原视频，569 帧通过官方 Real-ESRGAN 模型增强；自然比例裁切后制作 305 帧的静音 HEVC 双屏循环与同源封面，短叠化有瞬时重影。

任一屏触摸统一结束会话；原子退出标志及幂等清理释放播放器、纹理、EGL 目标和渲染线程，关闭两个屏保窗口。真实双屏恢复仍需设备验收。

54 种语言同步三选一规则。可执行检查覆盖三种随机结果、各 100 个固定循环、随机次数、共享时间线、停止后的回调、独立内容 ID、旋转及像素拼接。设备日志验收规则更新为会话内内容不变、三种选片结果和两种触摸退出。

当前仍是未签名且缺 B 的草稿，禁用启动。正式签名、63 设备安装、循环观察及触摸退出截图未完成，不能以单元测试替代实机验收。
